[build]
builder = "NIXPACKS"
buildCommand = "pip install -r requirements.txt"

[deploy]
startCommand = "python apps/obx_tasks/bot/main.py --mode live --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
